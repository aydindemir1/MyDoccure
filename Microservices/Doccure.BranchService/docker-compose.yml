services:
  mongo:
    image: mongo:latest
    container_name: doccure-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: